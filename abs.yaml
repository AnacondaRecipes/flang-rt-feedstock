build_parameters:
  - "--suppress-variables"
  - "--error-overlinking"

# Stage 2: Build with flang 21.1.8 from staging channel
# flang depends on libmlir21 which is now in pkgs/main
staging_channel_upload_target: llvm-21.1.8-stage2-build-0

channels:
  # Stage 2 channel has flang 21.1.8 (not yet in main)
  - https://staging.continuum.io/pbp/llvm-21.1.8-stage2-build-0
